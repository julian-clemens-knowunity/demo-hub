// Beat interval — every BEAT_MS, something gets destroyed.
export const BEAT_MS = 1500;
// When in the beat does the kick fire (object gets destroyed at this offset).
export const KICK_OFFSET_MS = 200;
// Lead-in fade for each scene before its first beat fires.
export const SCENE_FADE_IN_MS = 180;
